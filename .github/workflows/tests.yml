name: Tests

on:
  push:
  pull_request:

jobs:
  pest:
    name: PHP ${{ matrix.php }} / Laravel ${{ matrix.laravel }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - php: '8.1'
            laravel: '^10.0'
            testbench: '^8.0'
            pest: '2.36.0'
            phpunit: '10.5.36'
          - php: '8.2'
            laravel: '^11.0'
            testbench: '^9.0'
            pest: '^3.0'
            phpunit: ''
          - php: '8.3'
            laravel: '^12.0'
            testbench: '^10.0'
            pest: '^4.0'
            phpunit: ''
          - php: '8.4'
            laravel: '^13.0'
            testbench: '^11.0'
            pest: '^4.0'
            phpunit: ''
          - php: '8.5'
            laravel: '^13.0'
            testbench: '^11.0'
            pest: '^4.0'
            phpunit: ''

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: json
          coverage: none
          tools: composer:v2

      - name: Install dependencies
        run: |
          composer require \
            "illuminate/support:${{ matrix.laravel }}" \
            "illuminate/http:${{ matrix.laravel }}" \
            --no-interaction \
            --no-update
          composer require --dev \
            "orchestra/testbench:${{ matrix.testbench }}" \
            "pestphp/pest:${{ matrix.pest }}" \
            --no-interaction \
            --no-update
          if [ -n "${{ matrix.phpunit }}" ]; then
            composer require --dev \
              "phpunit/phpunit:${{ matrix.phpunit }}" \
              --no-interaction \
              --no-update
          fi
          composer update --prefer-dist --no-interaction --no-progress --with-all-dependencies --no-security-blocking

      - name: Run Pest
        run: composer test
